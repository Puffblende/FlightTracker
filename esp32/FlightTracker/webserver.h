#pragma once
// Forwarding stub — see ft_webserver.h for FlightTracker's config server.
// Uses #include_next so this file transparently forwards to Arduino's
// WebServer.h when found first on case-insensitive macOS filesystems.
#include_next <WebServer.h>
